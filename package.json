{
  "name": "roguelike-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean:hub-art": "node scripts/clean-hub-sprites.mjs",
    "clean:character-art": "node scripts/clean-character-sprites.mjs",
    "clean:art": "npm run clean:hub-art && npm run clean:character-art",
    "check:hub-layout": "node scripts/check-hub-layout.mjs",
    "check:art": "node scripts/check-art-assets.mjs",
    "check:anchors": "node scripts/check-weapon-anchors.mjs",
    "check:copy": "node scripts/check-hub-copy.mjs",
    "check:nav": "node scripts/check-hub-navigation.mjs",
    "check:b-line": "npm run check:hub-layout && npm run check:art && npm run check:anchors && npm run check:copy && npm run check:nav && npm run typecheck",
    "export:hub-preview": "node scripts/export-hub-layout-preview.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
