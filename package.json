{
  "name": "roguelike-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean:hub-art": "node scripts/clean-hub-sprites.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
